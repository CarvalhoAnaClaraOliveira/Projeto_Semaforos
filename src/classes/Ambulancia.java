package classes;

public class Ambulancia extends Veiculo {
    public Ambulancia(
        int id,
        String tipo, 
        String direcao,
        int velocidade,
        GeradorPlaca placa,
        Prioridade prioridade,
        String modelo
    ) {
        this.tipo = 'ambulancia';
        this.direcao =
    }
   |/*private void criarAmbulancia() {

        int numero = 1;

        for (int id = 0; ; id++) {

            for (int v = 4; v < 8; v++) {

                cruzamentos.add(

                        new Cruzamento(
                                numero,
                                ruas.get(h),
                                ruas.get(v)
                        )

                );

                numero++;

            }

        }

    } */ 
}
